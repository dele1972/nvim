" THEME DRACULA 

" *** Config Section ***
  if (has("termguicolors"))
    set termguicolors
  endif
  syntax enable

  colorscheme dracula
    " use transparent background (`:source ~/.config/nvim/init.vim` could help afterwards)
    "let g:dracula_colorterm=0
  "colorscheme gruvbox
  "  set background=dark
